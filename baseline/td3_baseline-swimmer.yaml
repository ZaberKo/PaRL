env: "Swimmer-v3"
gamma: 0.99999


num_rollout_workers: 0
num_eval_workers: 10

rollout_fragment_length: 1 # fixed

num_cpus_for_local_worker: 1
num_cpus_for_rollout_worker: 1

use_gpu: true

num_tests: 5
training_iteration: 3000
checkpoint_freq: 10
evaluation_interval: 2

# grad_clip: 10
